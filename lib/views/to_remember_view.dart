import 'package:flutter/material.dart';

class ToRememberView extends StatelessWidget {
  const ToRememberView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Expanded(
      child: Text('ToRemember View'),
    );
  }
}
